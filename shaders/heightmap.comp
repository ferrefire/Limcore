#version 450

#extension GL_ARB_shading_language_include : require

layout(set = 0, binding = 0, r16f) uniform image2D target;

layout(local_size_x = 8, local_size_y = 8, local_size_z = 1) in;

void main()
{
	imageStore(target, ivec2(gl_GlobalInvocationID.xy), vec4(0.5, 0.0, 0.0, 0.0));
}